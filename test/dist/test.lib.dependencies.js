/* * * * libstub-webpack-plugin: auto-generated manifest * * * */

const libstub = require('libstub');

libstub.export('uchain', require('uchain'));
libstub.export('vet', require('vet'));
libstub.export('moment', require('moment'));
